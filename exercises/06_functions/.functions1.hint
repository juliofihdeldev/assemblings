BL saves the return address in x30 (the Link Register).
RET jumps to the address stored in x30.

Without RET, after "mov x0, #42" the CPU keeps executing whatever
bytes follow get_answer — which is usually garbage or another function.

Add one instruction at the end of get_answer:

    ret

That's it. "ret" with no operands implicitly uses x30.
